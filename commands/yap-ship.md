# /yap ship — 安全提交 + 建立 PR

## 使用方式

```
/yap ship
/yap ship --bump patch
/yap ship --bump minor
/yap ship --bump major
```

## 行為

Invoke skill `yap-personal-harness:yap-ship`.

Pass `--bump` argument to the skill if provided.
